#blocksmenu
  %a{:href => "#", :id => "prev-block",
     :class => "block-menu", :title => "Go to previous block"}
  %a{:href => "#", :id => "next-block",
     :class => "block-menu", :title => "Go to next block"}
  - @nbslices.times do |i|
    %a{:href => "#", :class => "block-menu block-selector", :id => "bs#{i}"}
      %span= "#{i+1}"
#document
  - @results.each_slice_with_index(20) do |result_slice,index|
    %div{:id => "b#{index}", :class => "imgblock"}
      - result_slice.each_with_index do |r,index2|
        - iid = "i#{index}#{index2}"
        - @details_store <<  "details_store.#{iid} = { title:'#{r[:title]}', collection:'#{r[:collection]}', collection_url:'#{r[:collection_url]}', url:'#{r[:url]}', fullres_url:'#{r[:fullres_url]}'};\n"
        .imgcontainer{:id => iid}
          %img.thumb{:src => r[:thumb], :alt => r[:title]}
          -#%div.zindex z-index //DEVELOPMENT UTILITY
        - if (index2+1) % 5 == 0
          %hr.nextline
#tooltip
  %p#tooltip-title Image title

#details
  %p#details-title Image title
  %p#details-collection
    In
    %a Image collection
%img#details-image{:src => ""}
%img#details-image-back{:src => ""}
